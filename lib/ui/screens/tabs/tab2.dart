import 'package:flutter/material.dart';

class tab2 extends StatelessWidget {
  const tab2({super.key});
  static const String routeName ="tab2";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,
    );
  }
}